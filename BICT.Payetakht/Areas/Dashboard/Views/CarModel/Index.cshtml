@model IList<BICT.Payetakht.Data.ViewModels.CarModelViewModel>
@{
    ViewBag.DashboardTitle = "مدل";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}
<div class="table-responsive">
    <table class="table table-bordered table-condensed table-hover table-striped">
        <tr>
            <th>عنوان</th>
            <th>عنوان سازنده</th>
            <th></th>
        </tr>
        @foreach (var item in Model)
        {
            <tr>

                <td>@item.Title</td>
                <td>@item.CarManufactureTitle</td>
                <td>
                    
                    <div class="btn-group">
                        <a class="btn btn-info" href="@Url.Action("Edit","CarManufacture",new { id = item.ID })">ویرایش</a>
                        <a class="btn btn-danger" href="@Url.Action("Delete","CarManufacture",new { id = item.ID })">حذف</a>
                    </div>
                </td>
            </tr>
        }
    </table>
</div>
<a href="@Url.Action("Create")">ایجاد مدل جدید</a>
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}



