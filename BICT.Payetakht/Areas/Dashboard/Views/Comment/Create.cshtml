@model BICT.Payetakht.Data.ViewModels.CommentViewModel
@{
    ViewBag.DashboardTitle = "نظرات";
    Layout = "~/Views/Shared/AdminLayout.cshtml";
}
@using (Html.BeginForm())
{
    @Html.ValidationSummary("", new { @class = "text-danger" })
    <div class="form-group">
        @Html.LabelFor(m => m.Name, new { @class = " control-label" })
        <div class="">
            @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
        </div>
    </div>
    @Html.ValidationSummary("", new { @class = "text-danger" })
    <div class="form-group">
        @Html.LabelFor(m => m.Title, new { @class = " control-label" })
        <div class="">
            @Html.TextAreaFor(m => m.Title, new { @class = "form-control", id = "Summary" })
        </div>
    </div>
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <button type="submit" class="btn btn-default">ذخیره</button>
            <a href="@Url.Action("Index")">بازگشت</a>
        </div>
    </div>
    @section Scripts {
        @Scripts.Render("~/bundles/jqueryval");
        @Scripts.Render("~/Scripts/ckeditor/ckeditor.js")
        @Scripts.Render("~/Scripts/ckeditor/translations/fa.js")
        <script>
            ClassicEditor
                .create(document.querySelector('#Content'), {
                    language: 'fa',
                    toolbar: ['heading', '|', 'bold', 'italic', 'link', 'bulletedList', 'numberedList', 'blockQuote'],
                    direction: 'rtl'
                })
                .catch(error => {
                    console.error(error);
                });
        </script>
    }
}
